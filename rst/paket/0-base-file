version="0"
name="base-files"
mkdir -p $HOME/distro
cd $HOME/distro
rm -rf ${name}-${version}

mkdir -p ${name}-${version}

cd ${name}-${version}

mkdir -p run
mkdir -p run/udev
mkdir -p etc
mkdir -p etc/groups

cp $HOME/distro/${name}-${version}/* -rf $HOME/rootfs
